# 🗂️ Waste Detection System

Complete ML-powered waste detection system with camera overlay, backend API, and React frontend.

## 🚀 ONE-CLICK STARTUP

```bash
START_WASTE_DETECTION.bat
```

This will:
1. ✅ Install all dependencies
2. ✅ Create required directories  
3. ✅ Start ML service (Port 5001)
4. ✅ Start Backend API (Port 4000)
5. ✅ Start Frontend UI (Port 3000)
6. ✅ Open your browser automatically

## 🎯 System Features

- **Real-time camera detection** with YOLO
- **Violation alerts** with red borders
- **Detection logging** with SQLite
- **Billing calculations** with penalties
- **Secure React frontend** with protection

## 📱 Access Points

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **Camera Feed**: http://localhost:8000/video_feed

## ⚠️ Requirements

- Python virtual environment: `waste-management-venv`
- Node.js and npm installed
- Camera/webcam connected

## 🎮 Usage

1. Run `START_WASTE_DETECTION.bat`
2. Wait for all services to start
3. Browser opens automatically to http://localhost:3000
4. Navigate between Live View, Logs, and Billing pages
5. Place objects in front of camera to test detection

## 🛑 Shutdown

Close all 3 service windows when done.